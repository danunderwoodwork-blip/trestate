# Скопируйте в backend/.env и заполните под своё окружение.
# dev по умолчанию — SQLite; prod — PostgreSQL:
# TRE_DATABASE_URL=postgresql+psycopg2://user:password@host:5432/trestate
TRE_DATABASE_URL=sqlite:///./trestate.db
TRE_DEBUG=false
